# Port the NestJS API listens on
PORT=3000

# Comma-separated CORS allow-list for browser clients.
# When empty, all origins are allowed (dev default).
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
